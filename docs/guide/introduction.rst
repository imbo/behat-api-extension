Introduction
============

This guide will list all available steps the extension introduces, along with how they can be used together to test an API. The extension is aimed at testing JSON-based APIs, but can be used for other purposes as well.
